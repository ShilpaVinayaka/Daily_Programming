---Select the highest & lowest rating of the resort and display the output as "HIGHEST RATING" and "LOWEST RATING".
SELECT MAX(starRating) AS "HIGHEST RATING", MIN(starRating) AS "LOWEST RATING"
FROM Resort;
